[tool.black]
line-length = 120
target-version = ["py310"]

[tool.isort]
profile = "black"
line_length = 120
known_first_party = ["app"]
src_paths = ["app", "tests"]

[tool.ruff]
line-length = 120
target-version = "py310"
src = ["app", "tests"]

[tool.ruff.lint]
select = ["E", "F", "B"]
ignore = []
